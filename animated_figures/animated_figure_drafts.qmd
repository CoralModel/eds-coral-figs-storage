---
title: "animated_figure_drafts"
format: html
---

```{r}
library(tidyverse)
library(gganimate)
library(rgl)
library(rayshader)
library(rlang)
library(plotly)
library(ggimage)
```

```{r}
coral <- read_csv(here::here("..", "eds-coral-data-storage-management", "data", "volume_corals.csv"), na = c("UK", "Na", "?", "MK", "Remove, off transect in P9", "Unsampled", "unsampled", "NA", "US", "NS", "MissingObs", "HA", "Dead", "D", "dead", "v", "G")) %>% 
  mutate(y_scaled = y / 100,
         z_scaled = z / 100)
```
```{r}
coral_colors <- c(
    Acr = "blue", 
    Poc = "red", 
    Por = "darkgreen", 
    Mil = "darkorange"
  )


sph_vol <- function(l, w, h){
  v = ((l + w)/2)/2**3 * (4/3) * h
return(v)
}

cyl_vol <- function(l, w, h){
  v = (((l + w)/2)/2)**2 * 3.14 * h
return(v)
}




# Calculate volume of pocillopera:
coral_volume_poc <- coral %>% 
  filter(site == "LTER1",
         taxa == "Poc") %>% 
  rowwise() %>% 
  mutate(volume = sph_vol(length, width, height)) %>% 
  ungroup()

coral_volume <- coral %>% 
  filter(site == "LTER1",
         taxa != "Poc") %>% 
  rowwise() %>% 
  mutate(volume = cyl_vol(length, width, height)) %>% 
  ungroup()

coral_volume_mod <- rbind(coral_volume_poc, coral_volume)



```


```{r}
vol <- coral_volume_mod %>% 
  mutate(volume_scaled = volume / 100)

vol %>% 
  filter(site == "LTER1", habitat == "BR") %>% 
  ggplot(aes(x,y_scaled, group = coral_number)) +
  geom_point(aes(color = taxa, size = volume_scaled), show.legend = FALSE) +
  scale_color_manual(values = coral_colors) +
  facet_grid(taxa~transect) +
  theme(aspect.ratio = 0.2) +
  transition_states(year,
    transition_length = 2,
    state_length = 1) +
  labs(title = 'Year: {closest_state}')
```

```{r}
images <- list.files(path = here::here("animated_figures", "images"), pattern="png", full.names = TRUE, include.dirs =  TRUE)

img_df <- data.frame(taxa = c("Acr", "Mil", "Poc", "Por"),
                     image = images)

coral_img <- left_join(coral_volume_mod, img_df)
```
```{r}
coral_img %>% 
  filter(site == "LTER1" & 
           habitat == "BR" & 
           transect == "T03" &
           !is.na(length) &
           taxa != "Por") %>%
  ggplot(aes(x,y_scaled, group = coral_number)) +
  geom_image(aes(image = image, size = volume)) +
  theme_light() +
  theme(aspect.ratio = 0.2) +
  scale_size(range = c(0.02, 0.2), 
           name = "Volume (cm³)",
           trans = "log") +
  theme_light() +
  transition_states(year,
    transition_length = 2,
    state_length = 3) +
  transition_states(year,
    transition_length = 2,
    state_length = 1) +
  labs(title = 'Year: {closest_state}')
```

